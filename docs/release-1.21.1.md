# Albumium 1.21.1 (33)

- Album and card editors share object controls for every element type: forward/backward, first/last layer, rotation, scale/reset, alignment, fine movement, lock, duplicate and delete.
- Forward/backward buttons are directly visible; a separate Layers tab exposes all four stacking actions.
- Special-card studio also supports handwriting and embedded occasion cards, including their text editor. Multiple selected photos are all imported.
- Object insertion tools scroll horizontally instead of shrinking into narrow buttons.

Validation: 11 focused controls and card-layout tests passed. Full suite and device testing omitted to keep checks short.
Build: flutter build appbundle --release with the existing upload signing configuration.
